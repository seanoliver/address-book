---
description: Search durable project memory before investigating
argument-hint: "<topic>"
---

Search this repository's durable memory for `$@`.

Read `docs/README.md`, then search relevant entries under `docs/bugs/`, `docs/investigations/`, `docs/adr/`, `docs/runbooks/`, and `docs/plans/`. Read the strongest matches, summarize what is already known with file-path citations, and identify what still needs verification. Treat code, tests, configuration, and the issue tracker as authoritative; plans are intent only.
