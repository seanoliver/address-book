---
description: Capture a durable lesson from the current work
argument-hint: "[bug|investigation|adr|runbook|plan] [topic]"
---

Capture durable project memory for `${@:-the non-trivial work just completed}`.

Load and follow the `project-memory` skill. Search for an existing entry first, route the knowledge to one primary home, verify claims against authoritative sources, use that directory's template, and cross-link instead of duplicating information.
